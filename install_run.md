First run below command in terminal to install the requirements material/packages to the run this app.

===================================

pip install -r requirements.txt

===================================

after installation run below command to run this app.

===================================

streamlit-app run app.py

===================================